version: '2.3'

services:
  federated_gateway:
    ports:
      - "4000:80"
    build: .
    restart: unless-stopped
    networks:
      - sehacity-backend-tier
    env_file: services.env


networks:
  sehacity-backend-tier:
    driver: bridge
    name: sehacity-backend-tier
